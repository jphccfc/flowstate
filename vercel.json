{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npx prisma migrate deploy; fi && npm run build",
  "crons": [
    { "path": "/api/cron/sharepoint-sync", "schedule": "*/15 * * * *" }
  ]
}
