"use client";
import { use, useEffect, useState } from "react";
import Link from "next/link";
type Selection = { site: string; library: string; folder: string }; const fieldLabels = { site: "Site", library: "Library", folder: "Folder" } as const; type Preview = { connectionState: string; message: string; itemCount: number };
export default function SharePointIntegrationPage({ params }: { params: Promise<{ id: string }> }) { const { id: organizationId } = use(params); const [selection, setSelection] = useState<Selection>({ site: "", library: "", folder: "" }); const [preview, setPreview] = useState<Preview | null>(null); const [error, setError] = useState<string | null>(null); const [connectionMessage, setConnectionMessage] = useState<string | null>(null);
 useEffect(() => { fetch(`/api/clients/${organizationId}/integrations/sharepoint`).then((response) => response.ok ? response.json() : null).then((data) => data && setSelection(data.sourceSelection)).catch(() => setError("Integration status could not be loaded.")); }, [organizationId]);
 async function previewImport(event: React.FormEvent) { event.preventDefault(); setError(null); const response = await fetch(`/api/clients/${organizationId}/integrations/sharepoint`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ sourceSelection: selection }) }); const data = await response.json(); if (!response.ok) { setError(data.error ?? "Preview could not be created."); return; } setPreview(data); }
 return <main className="mx-auto w-full max-w-3xl p-4 sm:p-6"><Link href={`/clients/${organizationId}`} className="text-sm text-[var(--muted)]">&larr; Back to client</Link><div className="mb-6 mt-4"><div className="workspace-eyebrow mb-2">Integrations</div><h1 className="workspace-heading text-3xl font-bold">SharePoint</h1><p className="mt-2 text-sm text-[var(--muted)]">Choose an import source for reviewed, traceable evidence. This foundation does not connect to Microsoft Graph.</p></div><section className="workspace-card mb-6 p-4" aria-labelledby="connection-status"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 id="connection-status" className="font-semibold">Microsoft 365 connection</h2><p className="mt-1 text-sm text-[var(--muted)]">Not connected</p></div><button type="button" className="flowstate-accent-button rounded px-3 py-2 text-sm font-medium text-white" onClick={() => setConnectionMessage("Microsoft 365 connection setup is not available in this foundation. No Microsoft Graph or SharePoint connection is configured.")}>Connect Microsoft 365</button></div>{connectionMessage && <p role="status" aria-live="polite" className="mt-3 text-sm text-[var(--muted)]">{connectionMessage}</p>}<p className="mt-3 text-xs text-[var(--muted)]">No Microsoft Graph or SharePoint connection is configured. A future provider implementation must use an encrypted secret reference.</p></section><form onSubmit={previewImport} className="workspace-card p-4" aria-labelledby="source-selection"><h2 id="source-selection" className="font-semibold">SharePoint source selection</h2><p className="mt-1 text-sm text-[var(--muted)]">Record the intended site, library, and folder before a provider is connected.</p><div className="mt-4 grid gap-4 sm:grid-cols-3">{(["site", "library", "folder"] as const).map((field) => <label key={field} className="text-sm font-medium capitalize">{fieldLabels[field]}<input value={selection[field]} onChange={(event) => setSelection({ ...selection, [field]: event.target.value })} className="mt-1 w-full rounded border border-[var(--card-border)] bg-[var(--card)] px-2 py-2 text-sm text-[var(--foreground)]" placeholder={`SharePoint ${field}`} /></label>)}</div><div className="mt-4 flex flex-wrap gap-3"><button type="submit" className="rounded border border-[var(--card-border)] px-3 py-2 text-sm font-medium text-[var(--foreground)]">Preview import</button><button type="button" disabled={true} className="flowstate-accent-button rounded px-3 py-2 text-sm font-medium text-white opacity-50">Sync now</button></div>{error && <p role="alert" className="mt-3 text-sm text-red-700">{error}</p>}{preview && <div role="status" className="mt-4 rounded border border-[var(--card-border)] bg-[var(--muted-bg)] p-3 text-sm"><strong>{preview.connectionState === "NotConnected" ? "Not connected" : "Preview"}</strong><p className="mt-1 text-[var(--muted)]">{preview.message} Items available: {preview.itemCount}.</p></div>}</form></main>; }
